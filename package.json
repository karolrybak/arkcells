{
  "name": "arkcells-monorepo",
  "version": "0.0.56",
  "private": true,
  "type": "module",
  "workspaces": [
    "arkcells",
    "docs",
    "cellforge"
  ],
  "scripts": {
    "test": "cd arkcells && bun install && bun run test",
    "build": "cd arkcells && bun run build -w arkcells"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/bun": "^1.3.8"
  }
}
